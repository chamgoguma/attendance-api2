# attendance-api
1. Repository 분리 & 파일 저장 기능

StudentRepository

save(), load() 완성

Gson 적용, JSON 파일로 데이터 저장/로드 성공

2. Service 리팩토링

StudentService 생성자에서 자동 로드

출석/결석/지각 처리 로직 완성

update, findById, findAll 등 핵심 기능

3. Controller 구축

CLI 메뉴 완성

입력 검증(readInt)

모든 기능 연결

화면 출력 포맷 정리

4. 메인 실행 파일 연결

AttendanceApp에서 Controller start() 실행
